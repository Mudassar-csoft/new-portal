<div id="{{ $id }}" class="{{ $loaderClass ?? 'follow-loader' }}">
    <div class="{{ $spinnerClass ?? 'follow-spinner' }}">
        <div class="dot"></div>
        <div class="dot"></div>
        <div class="dot"></div>
    </div>
    <p>{{ $message }}</p>
</div>
